GEMINI_API_KEY=
# Private/local only: allow falling back to server GEMINI_API_KEY.
# On Vercel/public hosts prefer leaving this unset and using Settings → device key (BYOK),
# or accept that anyone who can POST /api/gemini can spend your quota.
# DIARY_ALLOW_SERVER_KEYS=1
# Only this Google account can Connect (comma-separated for multiple).
NEXT_PUBLIC_ALLOWED_GOOGLE_EMAIL=you@gmail.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
