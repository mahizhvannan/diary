# Product feedback (recorded)

## Done / in progress this pass
- Chat should see custom variables (height, weight → BMI).
- Larger chat textbox; New session more visible.
- Lazy-load older sessions/days on login (batch + scroll, capped).
- Remove Analyze tab for now.

## Your todos (not implemented yet)
5. **Architecture** — Review whether the app resembles LLM + tool-calling; make skills/modes more generalisable (dispatcher + tools instead of a long `if/else` of Gemini modes).
6. **Default chat prompt** — Review the prompt visible to / used by the default chat window for clarity, tone, and what context it receives.
